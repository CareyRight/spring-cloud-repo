spring:
  application:
    name: turbin
    
server:
  port: 5001
  
eureka:
  client:
    service-url:
      defaultZone: http://eureka1:2001/eureka, http://eureka2:2002/eureka
      
#配置监测的服务应用,并制定该聚合监控的名称(固定写法)    
turbine:
  app-config: order-service, zuul
  cluster-name-expression: new String("default")
